# Where the real, production que-app lives — used for "start the app" / TWA
# links, not the phone-mockup showcase (that's a static build, see
# scripts/build-app-showcase.mjs). Override for local dev against a que-app
# dev server:
PUBLIC_APP_ORIGIN=http://localhost:5173
PUBLIC_WAITER_ORIGIN=http://localhost:5174/waiter
